*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Проектная документация — хранится в отдельном месте, не в репо.
/TODO.md
/task.md
/PLANS.md

# Сгенерированные Room-схемы (не нужны, пока не появятся миграции).
/app/schemas/

# Keystore и пароли для подписи release-APK. Эти файлы НИКОГДА не должны попадать в репозиторий.
# Положи их локально (и в GitHub Secrets на CI) — см. README → «Сборка release APK».
keystore.properties
keystore/
*.jks
*.keystore